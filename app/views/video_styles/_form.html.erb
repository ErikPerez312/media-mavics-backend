<%= form_with(model: video_style, remote: false) do |form| %>
  <% if video_style.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% video_style.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :video_style %>
  </div>
  <div class="field">
    <%= form.label :video_url %>
    <%= form.text_field :video_url, id: :video_style %>
  </div>
  <div class="field">
    <%= form.label :thumbnail_url %>
    <%= form.text_field :thumbnail_url, id: :video_style %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
